apiVersion: v1
kind: Service
metadata:
  name: merchant-gateway
  namespace: payment-demo
  labels:
    app.kubernetes.io/name: merchant-gateway
    app.kubernetes.io/component: api-gateway
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: merchant-gateway
